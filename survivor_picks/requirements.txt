# The fetch layer is stdlib-only by design - no install needed to pull data.
# These are required by the pick models (scripts/recommend.py) and for
# downstream analysis of the fetched CSVs.
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
